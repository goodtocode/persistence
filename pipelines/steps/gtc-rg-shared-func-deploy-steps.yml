parameters:
- name: armPath
  type: string 
  default: '.'
- name: archivePath
  type: string 
- name: archiveFile
  type: string 
- name: appcsKey
  type: string
  default: 'AppSettingsConnection'
- name: appcsValue
  type: string
- name: envKey
  type: string
  default: 'ASPNETCORE_ENVIRONMENT'
- name: envValue
  type: string
  default: 'Production'
- name: funcName
  type: string
- name: subscriptionId
  type: string 
- name: subscriptionName
  type: string 

steps:
    - task: DownloadBuildArtifacts@0
      inputs:
        buildType: 'current'
        downloadType: 'single'
        artifactName: 'drop'
        downloadPath: '$(System.ArtifactsDirectory)'

    - task: AzureRmWebAppDeployment@4
      displayName: 'Deploying $(funcName)'
      inputs:
        ConnectionType: 'AzureRM'
        ConnectedServiceName: $(subscriptionName)
        subscriptionName: $(subscriptionId)
        appType: 'webApp'
        WebAppName: '$(funcName)'
        packageForLinux: '$(archivePath)/**/$(archiveFile)'
        AppSettings: '-$(envKey) $(envValue) -$(appcsKey) $(appcsValue)'